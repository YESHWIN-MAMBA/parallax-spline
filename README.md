Tech stack I'm using

- Vite (dev server + build tool)
- Vanilla JavaScript (ES modules) for UI + logic (your sections are JS template functions, not React components)
- Three.js for the 3D scene (WebGL)
- CSS stylesheet (src/styles.css)
- JSON-driven content (src/data/sections.json) imported into JS
- GitHub Pages for hosting (static build output)

So it’s a static SPA-like page (single HTML page) built with Vite + vanilla JS, with a Three.js canvas behind it.
